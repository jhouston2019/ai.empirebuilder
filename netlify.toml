[build]
  command = "npm run build"
  publish = ".next-build"
  functions = "netlify/functions"

# Removed redirect - Netlify handles function routes automatically
# [[redirects]]
#   from = "/.netlify/functions/*"
#   to = "/.netlify/functions/:splat"
#   status = 200

[build.environment]
  NODE_VERSION = "18"

